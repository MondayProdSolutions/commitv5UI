node_modules/
.next/
.env
.env.local
*.log

# SDD workspace (git-ignored scratch)
.superpowers/

# Git worktrees (isolated workspaces for feature branches)
.worktrees/

# Claude Code / agent tooling caches (skills, worktrees, lockfiles) — local
# dev-tool state, not part of the ERP application.
.claude/
.agents/
skills-lock.json

# Local embedded Postgres
.pgdata/
.e2e-db-url
.env.test.local

# Attendance photos storage
.attendance-photos/

# Build / tooling artifacts
/out/
/build
/coverage
*.tsbuildinfo

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
next-env.d.ts
.vercel
.DS_Store
